# Elite Electron Client

A cross-platform desktop client for Transformice(also DeadMaze), built on Electron.

## Development*

```bash
npm install
npm run tsc
npm start
```

## Build

```bash
npm run buildlnx   # Linux AppImage
npm run buildwin   # Windows
npm run buildmac   # macOS
```
